{
    "tools": [
        {
            "type": "function",
            "function": {
                "name": "set_light_state",
                "description": "Set the current state and brightness of lights",
                "parameters": {
                    "type": "object",
                    "properties": {
                    "brightness": {
                        "type": "integer",
                        "description": "The brightness to set the lights to, e.g. 30, 50 or 80"
                    },
                    "state": {
                        "type": "string",
                        "description": "The state to set the lights to, either 'on' or 'off'",
                        "enum": ["on", "off"]
                    }
                    },
                    "required": ["state"]
                }
            }
        }
    ]
}